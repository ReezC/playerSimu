# 基础（P0 收流链路必需）
av>=12.0.0
numpy>=1.26
opencv-python>=4.9
PyYAML>=6.0

# 本机负载清点（core/machineload.py）：训练/并行标注与实时预览同时跑时，推理的
# 前向会与输入尺寸无关地整体变慢，状态行靠它当场说出来。没装也能跑 —— 只是清不出
# 并行子进程，只剩内存那两项。
psutil>=5.9

# 数据落盘
pandas>=2.2
pyarrow>=15.0

# 感知阶段（体积大，装 CUDA 版 torch 前请单独处理）
# torch>=2.2
# torchvision>=0.17
# ultralytics>=8.1

# 本地窗口采集：WGC（Windows Graphics Capture，走 DWM，替代 BitBlt）
wgc-python>=2.0

# 可选：NDI 方案
# ndi-python
